swagger: "2.0"
info:
  title: twin-api
  version: 1.0.0

paths:
  /chat:
    post:
      operationId: chat
      x-google-backend:
        address: REPLACE_WITH_FUNCTION_URL
      responses:
        200:
          description: OK

    options:
      operationId: corsChat
      x-google-backend:
        address: REPLACE_WITH_FUNCTION_URL
      responses:
        200:
          description: CORS support